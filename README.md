# git-onekey-push
One-Key Git Push — 一键完成 git add、commit 和 push
```bash
wget https://raw.githubusercontent.com/MC090610/git-onekey-push/main/git-push.sh

# 下载后赋予执行权限
chmod +x git-push.sh
```

由于我太懒，于是就写了个sh 
其实是懒癌犯了~~（我不想每次更新blog还要输一堆git）~~
本sh完全由ai编写，所以不可避免有屎山（比如各种满天飞的if）
